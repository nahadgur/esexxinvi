// Testimonials component preserved as a no-op shell to avoid import breaks.
// We do not publish testimonials. Per UK DMCCA 2024 / CMA fake-review guidance,
// only verifiable practice data (tier, address, lead clinician, opening hours,
// treatment range) is shown across the site. Real patient stories live at
// /success-stories/ once collected via the structured /share-your-story/ form.

export function Testimonials(_props: { limit?: number; className?: string }) {
  return null;
}
